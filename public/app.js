const BASE=(window.__FG2?.base||"").replace(/\/$/,"");
const PAGE=24;
const S={all:window.__FG2?.products||[],q:"",brand:"Todas",cat:"Todas",limit:36};
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)], norm=v=>String(v||"").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g," ").trim();
const ars=v=>new Intl.NumberFormat("es-AR",{style:"currency",currency:"ARS",maximumFractionDigits:0}).format(v||0);
function match(p){if(S.brand!=="Todas"&&p.brand.name!==S.brand)return false;if(S.cat!=="Todas"&&!p.categorySlugs.includes(S.cat))return false;const terms=norm(S.q).split(" ").filter(Boolean), text=norm([p.name,p.brand.name,p.line,p.primaryCategory,...p.categorySlugs,p.description].join(" "));return terms.every(t=>text.includes(t))}
function score(p){const q=norm(S.q);let s=0;if(q&&norm(p.name).includes(q))s+=8;if(q&&p.categorySlugs.some(c=>q.includes(norm(c))||norm(c).includes(q)))s+=10;if(p.discountPercent)s+=p.discountPercent/10;return s}
function wa(p){return`https://wa.me/5493417234000?text=${encodeURIComponent(`Hola Farmagreen Rosario, quiero consultar por ${p.brand.name} - ${p.name}.`)}`}
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]||c))}
function url(path){return `${BASE}${path}`}
function clearFilters(){S.q="";S.brand="Todas";S.cat="Todas";S.limit=36;$("#search").value="";render()}
function card(p){return`<article class="card"><a class="pic" href="${url(`/producto/${esc(p.slug)}/`)}"><img src="${esc(p.images.card)}" alt="${esc(p.name)}"></a><div class="meta"><span>${esc(p.brand.name)}</span>${p.discountPercent>0?`<b>${Math.round(p.discountPercent)}%</b>`:""}</div><h3><a href="${url(`/producto/${esc(p.slug)}/`)}">${esc(p.name)}</a></h3><small>${esc(p.line)}</small><div class="price">${p.discountPercent>0?`<s>${ars(p.listPrice)}</s>`:""}<strong>${ars(p.offerPrice||p.listPrice)}</strong></div><a class="ask" href="${wa(p)}">Consultar</a></article>`}
function render(){const items=S.all.filter(match).sort((a,b)=>score(b)-score(a));const shown=Math.min(S.limit,items.length);$("#grid").innerHTML=items.slice(0,shown).map(card).join("");$("#count").textContent=`${shown} de ${items.length} productos`;const more=$("#loadMore");if(more){const left=Math.max(items.length-shown,0);more.hidden=left===0;more.textContent=left>PAGE?`Ver ${Math.min(PAGE,left)} más`:`Ver los ${left} restantes`;}$$("[data-brand]").forEach(b=>b.classList.toggle("on",b.dataset.brand===S.brand));$$("[data-cat]").forEach(b=>b.classList.toggle("on",b.dataset.cat===S.cat));const p=new URLSearchParams();if(S.q)p.set("q",S.q);if(S.brand!=="Todas")p.set("marca",S.brand);if(S.cat!=="Todas")p.set("cat",S.cat);history.replaceState(null,"",`${url("/catalogo/")}${p.toString()?`?${p}`:""}#productos`)}
function boot(){const p=new URLSearchParams(location.search);S.q=p.get("q")||"";S.brand=p.get("marca")||"Todas";S.cat=p.get("cat")||"Todas";$("#search").value=S.q;$("#search").addEventListener("input",e=>{S.q=e.target.value;S.brand="Todas";S.cat="Todas";S.limit=36;render()});$("#clearFilters")?.addEventListener("click",clearFilters);$("#loadMore")?.addEventListener("click",()=>{S.limit+=PAGE;render()});$$("[data-brand]").forEach(b=>b.onclick=()=>{S.brand=b.dataset.brand;S.q="";S.cat="Todas";S.limit=36;$("#search").value="";render()});$$("[data-cat]").forEach(b=>b.onclick=()=>{S.cat=b.dataset.cat;S.q="";S.brand="Todas";S.limit=36;$("#search").value="";render()});render()} boot();
